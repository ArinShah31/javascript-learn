const gameName = "Arin Shah";
const score = 100;
console.log(`Hello my name is ${gameName} and my score is ${score}`);

console.log(gameName[0]); //Accessing characters in a string using index
console.log(gameName.__proto__); //Accessing prototype of string

const url = "https://www.codevolu-tion.com";
console.log(url.replace("-", "")); //Replacing - with empty string

console.log(url.includes("arin")); //Checking if string includes a substring

const newString = "    Hello World    ";
console.log(newString.trim()); //Trimming whitespace from both ends of the string

console.log(newString.length); //Length of the string